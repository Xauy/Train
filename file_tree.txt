chameleon/
├── main.py                              # Точка входа: запускает QApplication + MainWindow
│
├── models/                              # [NEW] 3D-модели вагонов по умолчанию (.obj)
│   ├── README.md                        #   Инструкция: какие файлы класть, под какими
│   │                                    #     именами, в каком приоритете
│   ├── cisterna.obj                     #   ── ожидаемые файлы (опциональные) ──
│   ├── kryty.obj                        #   Если файл присутствует — он автоматически
│   ├── poluvagon.obj                    #     используется для вагонов соответствующего
│   ├── platforma.obj                    #     типа, у которых не задан явный путь.
│   ├── dumpkar.obj                      #   Если отсутствует — fallback Box.
│   ├── hopper.obj                       #   Сопоставление «тип → имя» хранится в
│   └── transporter.obj                  #     physics/wagon_defaults.DEFAULT_MODEL_FILES.
│
├── storage/
│   ├── __init__.py
│   ├── models.py                        # ProjectData, PathRecord, DKPRecord, WagonRecord,
│   │                                    # TrainRecord, ScenarioRecord, ScenarioStep, AxleDef,
│   │                                    # WagonSequenceEntry — хранение и сериализация
│   ├── project_store.py                 # ProjectStore.save() / .load() — низкоуровневый I/O
│   ├── project_io.py                    # [DONE 4.3] ProjectIO.save() / .load() /
│   │                                    #   .validate_structure() — высокоуровневый фасад
│   └── wagon_repository.py              # load_wagons() / save_wagons() — JSON-персистенция
│                                        #   библиотеки вагонов (wagon_library.json);
│                                        #   используется WagonsDialog
│
├── physics/
│   ├── __init__.py
│   ├── models.py                        # [DONE 2.2–2.3, UPDATED OBJ] AxleDef, WagonDef,
│   │                                    # WagonSlot, TrainConfig, DKPConfig, ScenarioStep,
│   │                                    # SimConfig, AxisState, DKPEvent, SimState.
│   │                                    # [NEW] WagonDef.model_path — путь к .obj-ассету
│   │                                    #   (опционально); сериализуется в to_dict/from_dict;
│   │                                    #   сохраняется при mirrored-варианте в expand()
│   ├── wagon_defaults.py                # [NEW] Сопоставление тип вагона → имя файла
│   │                                    #   в папке models/.  Без Qt.
│   │                                    #   Публичный API:
│   │                                    #     • DEFAULT_MODEL_FILES — словарь
│   │                                    #       {«Цистерна»: 'cisterna.obj', …}
│   │                                    #     • default_model_path(wagon_type) →
│   │                                    #       абс. путь, если файл существует, иначе None
│   │                                    #     • resolve_model_path(explicit_path, wagon_type)
│   │                                    #       — приоритет: явный путь → дефолт → ''
│   │                                    #     • supported_wagon_types() / expected_filename()
│   │                                    #   Используется MainWindow._build_sim_config.
│   └── simulation_engine.py             # [DONE 2.4–2.6] SimulationEngine(QObject):
│                                        #   Signals: tick_updated, dkp_triggered,
│                                        #            step_changed, sim_finished
│                                        #   API: start(), pause(), resume(), stop(),
│                                        #        get_state(), get_event_log()
│                                        #   Internal: _tick() — kinematics (Б),
│                                        #     axis update (В–Г), DKP crossing (Д),
│                                        #     step advance (Е), state emit (Ж)
│                                        #   validate() — pre-start integrity checks
│
├── validation/
│   ├── __init__.py
│   └── validators.py                    # validate_dkp_sensors(), validate_paths(),
│                                        # validate_scenario_steps() — чистые функции без Qt
│
└── ui/
    ├── __init__.py
    ├── main_window.py                   # [UPDATED 4.4 + 3.6 + OBJ + Defaults + Axles]
    │                                    #   MainWindow:
    │                                    #   QMenuBar «Файл» (Новый/Открыть/Сохранить/
    │                                    #   Сохранить как), self._current_file,
    │                                    #   заголовок окна, делегирование в ProjectIO;
    │                                    #   [3.6] кнопка «Симуляция»,
    │                                    #   _on_simulation_clicked() → SceneWindow,
    │                                    #   _build_sim_config() → SimConfig,
    │                                    #   _collect_path_data() → List[dict].
    │                                    #   [OBJ] _build_sim_config() читает
    │                                    #   row["model_path"] и row["type"] из SostavPage,
    │                                    #   передаёт их в resolve_model_path() и записывает
    │                                    #   итоговый путь в WagonDef.model_path
    │                                    #   (явный путь → дефолт по типу → '' / Box).
    │                                    #   [Axles] Если row["axles"] непуст — оси берутся
    │                                    #   из него; иначе сохраняется прежний 2-bogie
    │                                    #   fallback от base/length (backwards-compat).
    ├── scene_window.py                  # [UPDATED 3.1–3.5 + Couplers + OBJ]
    │                                    #   SceneWindow(QMainWindow):
    │                                    #
    │                                    #   ── Module-level helpers ─────────────────
    │                                    #   _PathGeom — кэш геометрии одного прямого
    │                                    #     отрезка; s_to_xyz(), s_to_xyz_off(),
    │                                    #     rail_z() — интерполяция вдоль сегмента
    │                                    #
    │                                    #   [NEW OBJ] _load_obj_mesh(path) →
    │                                    #     (vertices, faces) | None.
    │                                    #     • Кэш _OBJ_CACHE (по абсолютному пути)
    │                                    #       — каждый файл парсится один раз.
    │                                    #     • Если установлен trimesh → используется
    │                                    #       он (force='mesh', process=False) —
    │                                    #       поддержка quads, отрицательных индексов,
    │                                    #       групп, материалов.
    │                                    #     • Иначе → встроенный _parse_obj_simple():
    │                                    #       минимальный pure-Python парсер v/f-строк
    │                                    #       с триангуляцией веером.
    │                                    #     • Тихий fallback к Box, если файл
    │                                    #       отсутствует / нечитаем / некорректен.
    │                                    #
    │                                    #   [NEW OBJ] _prepare_wagon_mesh_verts(verts,
    │                                    #     target_length_mm) — центрирует bbox в
    │                                    #     origin и равномерно масштабирует так,
    │                                    #     чтобы самая длинная ось совпала с
    │                                    #     длиной вагона; пропорции сохраняются.
    │                                    #     Соглашение об ориентации модели:
    │                                    #     X = вдоль вагона, Y = поперёк, Z = вверх
    │                                    #
    │                                    #   ── DKP visuals ──────────────────────────
    │                                    #   _DKPVisual — shaft + diamond head + halo +
    │                                    #     счётчик осей (Text); flash() / reset_flash()
    │                                    #     / reset_counter()
    │                                    #
    │                                    #   ── SceneWindow ──────────────────────────
    │                                    #   VisPy SceneCanvas — TurntableCamera,
    │                                    #   _init_ground_grid() — сетка пола,
    │                                    #   _init_tracks() — рельсы + шпалы + метка пути,
    │                                    #   _init_dkp_markers() — _DKPVisual per sensor,
    │                                    #   _init_wagon_visuals() — для каждого вагона
    │                                    #     вызывает _make_wagon_visual() и добавляет
    │                                    #     маркеры осей; затем строит coupler_line.
    │                                    #
    │                                    #   [NEW OBJ] _make_wagon_visual(wagon, ...) —
    │                                    #     если wagon.model_path задан и _load_obj_mesh
    │                                    #     вернул геометрию → visuals.Mesh с
    │                                    #     shading='smooth', цвет _WAGON_BODY;
    │                                    #     иначе → исходный visuals.Box.
    │                                    #     Оба варианта получают одинаковый
    │                                    #     MatrixTransform (rotate Z + translate),
    │                                    #     поэтому _on_tick() работает единообразно.
    │                                    #
    │                                    #   _on_tick(state) — boundary check,
    │                                    #     обновление позиций каждого _wagon_visuals[i],
    │                                    #     маркеров осей и coupler_line + HUD,
    │                                    #   _check_boundary() — пауза при выходе за концы,
    │                                    #   _coupler_points() — полилиния по торцам,
    │                                    #   _on_dkp_triggered() — вспышка 320 мс,
    │                                    #   _on_step_changed() / _on_sim_finished(),
    │                                    #   Кнопки: Пуск / Пауза (тоггл) / Стоп;
    │                                    #   4 состояния: Idle / Running / Paused / Done;
    │                                    #   canvas @property — для MP4-экспорта (раздел 5)
    │                                    #
    │                                    #   Зависимости: vispy + pyopengl (обязательны);
    │                                    #     trimesh (опционально, улучшает совместимость
    │                                    #     OBJ; без него работает встроенный парсер)
    ├── pages/
    │   ├── __init__.py
    │   ├── base_page.py                 # BasePage(QWidget) — абстрактный контракт
    │   │                                #   to_dict() / from_dict()
    │   ├── paths_page.py                # PathsPage — редактор путей;
    │   │                                #   to_dict() / from_dict() [4.2 ✓ уже реализованы]
    │   ├── dkp_page.py                  # DkpPage — редактор ДКП;
    │   │                                #   to_dict() / from_dict() [4.2 ✓ уже реализованы]
    │   ├── sostav_page.py               # [UPDATED Axles] SostavPage — редактор состава;
    │   │                                #   to_dict() / from_dict() [4.2 ✓ уже реализованы];
    │   │                                #   скрытые колонки:
    │   │                                #     _COL_MODEL — model_path (.obj),
    │   │                                #     _COL_TYPE  — тип вагона (для дефолтной модели).
    │   │                                #   Обе заполняются из WagonsDialog
    │   │                                #   (selected_model / selected_type).
    │   │                                #   Параллельное хранение self._row_axles:
    │   │                                #   list[list[dict]] — оси на строку
    │   │                                #   (как _steps_data в ScenarioPage).
    │   │                                #   to_dict()["axles"] передаёт список осей
    │   │                                #   в MainWindow._build_sim_config.
    │   └── scenario_page.py             # ScenarioPage — список сценариев;
    │                                    #   to_dict() / from_dict() [4.2 ✓ уже реализованы]
    └── dialogs/
        ├── __init__.py
        ├── scenario_steps_dialog.py     # ScenarioStepsDialog — шаги сценария;
        │                                #   get_steps() / set_steps() [4.2 ✓ уже реализованы]
        ├── axle_positions_dialog.py     # [NEW] AxlePositionsDialog — редактор положения
        │                                #   осей одного вагона.  Принимает count и
        │                                #   initial_axles=list[{"offset_mm": float}];
        │                                #   на accept get_axles() возвращает обновлённый
        │                                #   список.  Опционально wagon_length_mm —
        │                                #   валидирует, что offset не превышает длину.
        │                                #   Полностью развязан с WagonsDialog — работает
        │                                #   с plain-dict’ами.
        └── wagons_dialog.py             # [UPDATED Axles] WagonsDialog — браузер вагонов:
                                         #   ── Колонки таблицы ─────────────────────
                                         #   ID, Название, Тип, База, Длина, Высота,
                                         #   Кол-во осей (QSpinBox 0–32),
                                         #   Оси (QPushButton → AxlePositionsDialog),
                                         #   3d модель (QPushButton → QFileDialog),
                                         #   Удалить.
                                         #   ── Параллельное хранение ───────────────
                                         #   self._row_axles: list[list[dict]] — список
                                         #   осей для каждой строки таблицы; синхронизируется
                                         #   с QSpinBox (растёт/обрезается, сохраняя
                                         #   существующие смещения).
                                         #   ── Persistence ─────────────────────────
                                         #   wagon_library.json теперь хранит для каждого
                                         #   вагона поля "axle_count" и "axles".
                                         #   Backwards-compat: старые файлы без этих ключей
                                         #   читаются как axle_count=4, axles=[0,0,0,0].
                                         #   ── Возвращаемые значения ───────────────
                                         #   selected_id / _name / _type / _base / _length /
                                         #   _height / _model / _axles (list[dict]) →
                                         #   передаются в SostavPage._row_axles →
                                         #   to_dict() → MainWindow → WagonDef.axles

